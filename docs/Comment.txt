[v3.4 TotalSegmentator-init candidate, 2026-07-28] Two-stage STU-Net-B cascade for PENGWIN 2026 Task 1. Stage A is the refreshed-data Dataset539 anatomy model trained from scratch (PengwinTrainerSTUNetBaseAnatomyV301, fold 0). The hybrid v3.3 family router is retained: a random forest is primary when confident (|p_femur-0.5| >= 0.15), with the organizers' official acquisition rule used only as an uncertainty tiebreak. Stage B is the new Dataset538 V308 ABBC+9-affinity model initialized from the official TotalSegmentator base_ep4k backbone and further fine-tuned on the refreshed PENGWIN data (PengwinTrainerSTUNetBaseAffinityV308DeployedVal, fold 0). Its 13-channel output is decoded by average-linkage agglomeration at the validation-selected threshold T=0.75 and mapped to the official label ranges. On the same 68-case official-aligned local evaluation, this candidate reached Dice 0.8821, HD95 3.261 mm, ASSD 0.775 mm, instance F1 0.9301, and 426 split fragments. The Stage-B run ended after epoch 180 without checkpoint_final; the valid checkpoint_best is packaged. Existing model_v3_0 remains the rollback model until the candidate passes Grand Challenge container validation.
