================================================================
Grand Challenge - "Create Algorithm for Challenge" form fill
PENGWIN 2026 Task 1 Submission (V2.6 updated-data checkpoints)
================================================================

[Title] (single line, method identifier)
----------------------------------------------------------------
Anatomy-Routed ABBC-Affinity STU-Net Cascade (PENGWIN-Auto V2.6)
----------------------------------------------------------------
(method-level; reusable across container versions)


[Description] (max 1024 chars)
----------------------------------------------------------------
Pelvic and femoral fracture fragment instance segmentation in CT.

Two-stage cascade (two STU-Net-B models, loaded serially on one GPU):
1. Anatomy — Dataset539_PelvicFemurAnatomyV3 runs on the full CT, labelling Sacrum, Left/Right Hipbone and Femur (5-class).
2. Target-family routing — the pelvic/femur decision functions published by the PENGWIN organizers select the case family. Pelvic cases forward only Sacrum+LeftHip+RightHip to Stage B; femur cases forward only Femur.
3. Fracture — Dataset538 V308 ABBC+affinity STU-Net runs per target ROI using CT only. Stage-A anatomy is used only to localize the ROI. It emits ABBC channels plus learned same-instance affinities.
4. Decoding — average-linkage agglomeration yields instance masks; ~1 cm^3 components are pruned and outputs are remapped to the official PENGWIN label ranges.

Backbone STU-Net-B (Apache-2.0) warm-started from a TotalSegmentator bone pretrain. Femur is fully modelled. V2.6 deploys V301 and V308 fold-0 checkpoints retrained after rebuilding the refreshed official PENGWIN dataset; architecture, decoder, thresholds and post-processing remain unchanged.

Output: PENGWIN label map (0 bg; 1-50 sacrum; 51-100 L hip; 101-150 R hip; 151-200 femur).

GPU: T4 16 GiB. Peak = max(stage1, stage2), not sum (serial load); STU-Net-B ~4 GiB/stage.
----------------------------------------------------------------


[Job requires gpu type]
----------------------------------------------------------------
NVIDIA T4 Tensor Core GPU (16 GiB VRAM)

Reason: the two STU-Net-B models (Ds539 anatomy + Ds538 ABBC fracture) are
loaded/released serially, so peak VRAM = max(stage1, stage2) ~ 4 GiB, not sum.
Measured end-to-end runtime 45-205 s/case (3090), well within the T4 / 10-min
budget. T4 16 GiB has ample margin.

================================================================
Note: single algorithm slot; Title/Description are method-level and survive
future container swaps.
================================================================
